%h1= @store.name

= link_to 'Edit', edit_store_path(@store)
= link_to 'All Stores', stores_path
%p= @store.description
%h1= 'Available Pets'
- @store.pets.each do |pet|
  %ul
    %li= link_to pet.name, pet_path(pet)

